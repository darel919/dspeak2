import { createHybridMediaSessionLifecycle } from "./hybrid-media-session-lifecycle.js";

export function createHybridMediaSessionRuntime({
  authStore,
  buildMediaControlSocketUrl,
  channelsStore,
  connected,
  error,
  getIntentionalClose,
  getMediaControlUrl,
  getRoomId,
  getSfu,
  getSupabaseClient,
  handleMediaSignalingClose,
  handoff,
  iceConnectedBoth,
  lastInRoom,
  mediaConnectionState,
  mediaControlApiPath,
  mediaControlTicketState,
  mediaControlSocketUrlState,
  messageHandlers,
  participantSfuRoundTripTimes,
  protocolState,
  protocolUpdateRequired,
  providerRecovery,
  queueTopology,
  remoteProducersCount,
  resetMediaTelemetryState,
  resetTopologySequencing,
  runtimeConnectionTimeoutMs,
  setActiveProvider,
  setChannelId,
  setConnectionPhase,
  setIceServers,
  setIntentionalClose,
  setLocalPeerId,
  setMediaControlSocketUrl,
  setMediaControlTicket,
  setP2pMesh,
  setSfu,
  signaling,
  syncConnectedUsers,
  topologyState,
  transportReady,
  voiceStore,
  closeProviders,
  ensureP2p,
  ensureSfu,
  getChannelId,
  getDeviceId,
  getP2pMesh,
  getBootstrap,
  handleP2pQualification,
  handleProviderFailure,
  handleProviderTicket,
  mediaPathMetrics,
  peerConnectionMetrics,
  peerRoundTripTimes,
  receiveAttenuation,
  resetLifecycle,
  resolveTopologyWaiter,
  sfuProducerIds,
  sendParticipantVoiceState,
  sendSourceState,
  setTopologyWaiter,
  setupMessageHandlers,
  queueCloudflarePublication,
}) {
  return createHybridMediaSessionLifecycle({
    authStore,
    buildMediaControlSocketUrl,
    channelsStore,
    connected,
    error,
    getIntentionalClose,
    getMediaControlUrl,
    getRoomId,
    getSfu,
    getSupabaseClient,
    handleMediaSignalingClose,
    handoff,
    iceConnectedBoth,
    lastInRoom,
    mediaConnectionState,
    mediaControlApiPath,
    mediaControlTicketState,
    mediaControlSocketUrlState,
    messageHandlers,
    participantSfuRoundTripTimes,
    protocolState,
    protocolUpdateRequired,
    providerRecovery,
    queueTopology,
    remoteProducersCount,
    resetMediaTelemetryState,
    resetTopologySequencing,
    runtimeConnectionTimeoutMs,
    setActiveProvider,
    setChannelId,
    setConnectionPhase,
    setIceServers,
    setIntentionalClose,
    setLocalPeerId,
    setMediaControlSocketUrl,
    setMediaControlTicket,
    setP2pMesh,
    setSfu,
    signaling,
    syncConnectedUsers,
    topologyState,
    transportReady,
    voiceStore,
    mediaSessionSetup: {
      closeProviders,
      ensureP2p,
      ensureSfu,
      getChannelId,
      getDeviceId,
      getP2pMesh,
      getSfu,
      getBootstrap,
      handleP2pQualification,
      handleProviderFailure,
      handleProviderTicket,
      mediaPathMetrics,
      peerConnectionMetrics,
      peerRoundTripTimes,
      receiveAttenuation,
      resetLifecycle,
      resolveTopologyWaiter,
      sfuProducerIds,
      sendParticipantVoiceState,
      sendSourceState,
      setTopologyWaiter,
      setupMessageHandlers,
      queueCloudflarePublication,
    },
  });
}
